<script setup>
import { computed } from 'vue';

const props = defineProps({
    title: String,
    name: String,
    field: String,
    type: String,
})

const value = computed(() =>props.field);
</script>

<template>
    <div class="mb-6">
        <label for="confirm_password" class="block mb-2 text-sm font-medium text-gray-900 dark:text-white">
            {{ title }}
        </label>
        <input :type="type" v-model="value" id="confirm_password" class="bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500" placeholder="•••••••••" required>
    </div>
</template>